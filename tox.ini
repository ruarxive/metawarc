# Tox (http://tox.testrun.org/) is a tool for running tests
# in multiple virtualenvs.


[tox]
envlist = py39,py310,py311,py312


[testenv]
deps =
    -r requirements.txt
    -r requirements-dev.txt

commands =
    py.test --verbose {posargs:./tests}
